spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true           #开启Rest风格
  datasource:                         #数据库配置
    url: jdbc:mysql://localhost:3306/finance_platform?&serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
    username: root
    password: 20021002zt
    type: com.zaxxer.hikari.HikariDataSource              #连接池
    driver-class-name: com.mysql.cj.jdbc.Driver           #8.0数据库
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  redis:    #redis配置
    # redis数据库索引（默认为0），我们使用索引为3的数据库，避免和其他数据库冲突
    database: 0
    # redis服务器地址（默认为localhost）
    host: smartshift.top    #smartshift.top,localhost
    # redis端口（默认为6379）
    port: 6379
    # redis访问密码（默认为空）
    password: 20021002zt
    # redis连接超时时间（单位为毫秒）
    timeout: 2000
  mail:
    # 发送者邮箱
    username: 3056786023@qq.com
    #申请到的授权码
    password: gsdifaxjrsabdgbc
    # 配置 SMTP 服务器地址
    host: smtp.qq.com
    # 端口号465或587
    port: 465
    protocol: smtps
    # 默认的邮件编码为UTF-8
    default-encoding: UTF-8
    # 配置SSL 加密工厂
    properties:
      mail:
        smtp:
          socketFactoryClass: javax.net.ssl.SSLSocketFactory
          #表示开启 DEBUG 模式，这样，邮件发送过程的日志会在控制台打印出来，方便排查错误
          debug: true
          ssl: true
  servlet:
    multipart: #设置文件传输限制
      max-file-size: 100MB
      max-request-size: 100MB
mybatis-plus:           #mybatis-plus配置
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   #配置日志
  global-config:
    db-config:
      id-type: AUTO   #设置id填充策略
aliyun:               #阿里云配置
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com     #endponit
    keyId: LTAI5t7nBNVRNF5WnmsBWdRJ           #keyid
    keySecret: qykTPzYXPFCnBp1QE0KQPHt6NuFs8X  #keysecret
    bucketName: zt12

#server:
#  port: 8888